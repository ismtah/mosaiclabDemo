CREATE TABLE `PRODUCT` (
                        `ID` int(11) NOT NULL AUTO_INCREMENT,
                        `name` varchar(40) ,
                        `marque` varchar(40) ,
                        `type` varchar(40) ,
                        `price` DOUBLE
);